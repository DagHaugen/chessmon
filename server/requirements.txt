fastapi>=0.110
uvicorn[standard]>=0.29
# vision + chess engine come from the repo root: numpy, opencv-python, python-chess
# the WebRTC bridge (webrtc/rtc_peer.py) needs websockets + aiortc -- installed by `chessmon setup`
